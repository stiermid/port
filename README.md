# port

Minimal ZSH theme combining the `robbyrussell` layout with the `gentoo` git workflow.

## Install

```sh
cp port.zsh-theme ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/themes/port.zsh-theme
```

Set in `~/.zshrc`:

```sh
ZSH_THEME="port"
```

Reload:

```sh
source ~/.zshrc
```
